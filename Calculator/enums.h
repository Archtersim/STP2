#ifndef ENUMS_H
#define ENUMS_H

//tctrl
enum class Oper {
   None, Add, Sub, Mul, Div
};
enum Func {
   Rev, Sqr
};
//tmemory
enum class Commands {
    Store, Add, Clear, Copy
};
enum NumStates {
    OFF, ON
};
#endif // ENUMS_H
